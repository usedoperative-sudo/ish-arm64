name: Build iSH-ARM64 (Fork)

on:
  workflow_dispatch:

jobs:
  build-fork:
    runs-on: macos-14
    steps:
      - name: Checkout del repositorio con submódulos
        uses: actions/checkout@v3
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Instalar dependencias del sistema (Meson, Ninja, LLVM)
        run: |
          brew install ninja lld llvm meson

      - name: Parchear globalmente todas las variables atómicas con NULL en el código C
        run: |
          python3 -c '
          import glob, re
          
          # Patrón para encontrar declaraciones estáticas atómicas inicializadas a NULL
          # Ej: static _Atomic(tipo) nombre = NULL;
          pattern = re.compile(r"static\s+_Atomic\s*\([^)]+\)\s+\w+\s*=\s*NULL\s*;")
          
          for filepath in glob.glob("**/*.[ch]", recursive=True):
              try:
                  with open(filepath, "r", encoding="utf-8") as f:
                      content = f.read()
                  
                  new_content = pattern.sub(lambda m: m.group(0).replace(" = NULL", ""), content)
                  
                  if new_content != content:
                      with open(filepath, "w", encoding="utf-8") as f:
                        f.write(new_content)
                      print(f"Patcheado: {filepath}")
              except Exception as e:
                  pass
          '
          
      - name: Configurar y compilar el backend ARM64 con Meson
        run: |
          mkdir -p build-ios
          meson setup build-ios . -Dguest_arch=arm64 --buildtype=release
          ninja -C build-ios

      - name: Compilar la App de iOS sin firma
        run: |
          xcodebuild -workspace App.xcworkspace \
                     -scheme iSH \
                     -sdk iphoneos \
                     -configuration Release \
                     CODE_SIGN_IDENTITY="" \
                     CODE_SIGNING_REQUIRED=NO \
                     CODE_SIGNING_ALLOWED=NO \
                     build
